model Conversation {
  id                    Int                   @id @default(autoincrement())
  conversation_profiles ConversationProfile[]
  created_at            DateTime              @default(now())
  updated_at            DateTime?             @updatedAt

  @@map("conversations")
}
