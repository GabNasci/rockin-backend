model users {
  id         Int        @id @default(autoincrement())
  email      String     @unique
  password   String
  profiles   profiles[]
  created_at DateTime   @default(now())
  updated_at DateTime   @updatedAt
}
