model Genre {
  id         Int       @id @default(autoincrement())
  name       String
  bands      Band[]
  profiles   Profile[]
  created_at DateTime  @default(now())
  updated_at DateTime? @updatedAt

  @@map("genres")
}
